# Module Map

## Root Runtime Files

| File | Responsibility | Notes |
| --- | --- | --- |
| `server.js` | Main HTTP server, app-server client, auth, routes, thread detail compaction, target-turn Usage summary backfill from rollout scans, uploads, continuation jobs, Web Push, update/restart endpoints, thread visibility filtering including migrated Windows-cwd history on macOS, rollout-session fallback time/status inference including deferred thread-list status attachment after visible candidate filtering and active turn-id retention, newest-session-first rollout discovery under the candidate cap, per-pass archived id reuse, single-tail stale-active evidence reuse, bounded thread-list cold-path source counters for rollout discovery/head/status-tail and session-index volume/reuse, same-baseline session-index source-context reuse, fallback baseline final-filter/merge work counters, and thread-list app-server request-window policy / latency-attribution diagnostics, local-file preview roots, authenticated GitHub link-preview route, app-server thread goal set/action/list/detail decoration, inherited runtime model/effort application, server-side cross-thread task-card draft materialization from completed turns/thread-detail reads, server-side thread-display mode/pane-slot/split-pair runtime settings, the thread-callable direct task-card route gated by the runtime `跨工作区委派` setting, Codex app-server dynamic tool injection/`item/tool/call` handling for `codex_mobile.delegate_to_thread` and `codex_mobile.return_to_source`, automatic per-Codex-Home `codex_mobile` MCP toolset registration, workspace-delegation sandbox/profile wiring plus app-server approval decisions for dynamic source-write guard, and the disabled `workspace-delegation` compatibility route | Treat as composition glue. Extract reusable logic instead of expanding large inline blocks. |
| `codex-app-server-mux.js` | Shared Desktop/Mobile app-server bridge, endpoint publication, TCP server, app-server notification replay, approval proxying | Stdout is protocol data for Desktop; diagnostics must go to mux log. When it starts the real Codex CLI child, it must drop Desktop-bridge-only `CODEX_CLI_PATH` and `CODEX_MUX_*` env vars. |
| `codex-app-server-mux-shim.cs` | Windows `.exe` shim for Desktop `CODEX_CLI_PATH` | Rebuild/relaunch Desktop through the shared launcher after changes. The default generated shim is `codex-app-server-mux-win.exe` so older running `codex-app-server-mux.exe` processes do not block rebuilds. The shim itself must be compiled as `/target:winexe`; shim-spawned Node must use `CREATE_NO_WINDOW` and hidden startup flags so Desktop bridge helpers never open a console. |
| `start-codex-mobile-web*.ps1/.vbs` | Windows startup wrappers and hidden scheduled-task startup | User-logon task is preferred when WSL access is needed; LocalSystem `AtStartup` mode is supported for no-login boot and must map `USERPROFILE`/`HOME`/`APPDATA`/`LOCALAPPDATA`/`TEMP`/`TMP` back to the target user before resolving Codex state or binaries. Plugin HTTPS base URL, Hermes frame-origin settings, active Codex profile resolution, profile auth/config backup without replacement, non-default profile shared-state hardlink/junction setup, and clearing Desktop bridge env before Mobile-owned CLI startup must be preserved here for scheduled deployments. |
| `restart-codex-mobile-shared-chain.ps1` | Scoped restart for Mobile Web chain | Resolves the active profile `CODEX_HOME` before removing/waiting on the mux endpoint. Does not restart Hermes Mobile, WSL, Codex Desktop, or unrelated services. |
| `scripts/deploy-macos-plugin.ps1` | Windows-orchestrated Mac Hermes plugin production deploy harness | Deploys only a clean public git archive, rejects private/runtime paths, stages checks before target backup/sync, runs target checks before LaunchDaemon restart, and smokes status without printing key material. Keep covered by `test/macos-plugin-deploy-harness.test.js`. |
| `scripts/create-thread-task-card.js` | Local UTF-8-safe CLI wrapper for `POST /api/threads/:sourceThreadId/task-cards`, intended for Codex-thread/tool initiated delegation | Reads the Codex Mobile access key from env or runtime key file, never prints it. Direct source-thread approval is gated by the runtime `跨工作区委派` setting; otherwise cards stay pending. Supports bounded `--reasoning-effort` for deep audit cards. |
| `scripts/return-thread-task-card.js` | Local UTF-8-safe CLI wrapper for `POST /api/thread-task-cards/:id/reply`, intended for target-thread manual return cards | Reads the Codex Mobile access key from env or runtime key file, never prints it, creates a stable `task-card-return:*` idempotency key when the caller does not supply one, and marks replies as `returnToSource` so they are source-direct approved instead of ordinary pending replies. |
| `scripts/codex-mobile-mcp-server.js` | Codex-thread stdio MCP server exposing `list_threads`, `delegate_to_thread`, and `return_to_source` | Calls the authenticated local Codex Mobile HTTP API and never stores or prints raw key material. Registered automatically into all known/target Codex Homes by `syncKnownCodexMobileMcpToolsets()` and `syncCodexMobileMcpToolset()`, including tool-level `approval_mode = "approve"` entries. Exposes bounded `reasoningEffort` on delegation and terminal return statuses including `rejected` / `partially_completed`. |
| `scripts/codex-mobile-empty-detail-cache-smoke.js` | Home AI live-debug visual smoke for empty-detail DOM failure classes | Drives the embedded `__codexMobileVisualHarness.simulateEmptyCachedDetailOpen()` and `simulateStableSignatureEmptyDom()` hooks, verifies that the DOM reaches nonempty turn rows instead of `No visible turns.`, and writes only bounded screenshot/metadata artifacts. |
| `scripts/codex-mobile-image-order-visual-smoke.js` | Home AI live-debug visual smoke for uploaded/generated image-card ordering | Opens a bounded target thread through the live-debug lane and verifies image-card DOM timestamp ordering. Reports only metadata/hash fields such as endpoint kind, build ids, turn/item hashes, route kind, rects, timestamps, counts, screenshot path hash, and bounded error codes; it does not output raw thread/turn/item ids, debug URLs, screenshot paths, DOM text, upload contents, cookies, tokens, provider payloads, or logs. |
| `scripts/codex-mobile-media-render-visual-smoke.js` | Home AI live-debug visual smoke for uploaded/generated media rendering | Opens a bounded target thread through the live-debug lane and verifies visible `.input-image`, `.image-view`, `.markdown-image`, and file-preview image surfaces are present, visible, loaded, proxy-safe in Home AI embed mode, and free of raw local-path leaks. Reports endpoint kind, expected build/cache ids, hashed thread/turn/item/source ids, route-kind counts, image dimensions, rects, failure counts, screenshot path hash, and bounded error codes; it does not output raw image URLs, local paths, filenames, DOM text, uploads, cookies, tokens, provider payloads, or logs. |
| `scripts/codex-mobile-projection-replay-visual-smoke.js` | Home AI live-debug visual smoke for thread-detail projection/API shape versus DOM replay | Opens a bounded target thread, fetches the same thread detail through the embedded plugin/proxy-safe API path, and compares API visible-turn/item counts and latest/order hashes with actual `.turn[data-turn]`, `.item[data-item]`, and `data-render-key` DOM metadata. Reports endpoint kind, expected build/cache ids, hashed thread/turn/item ids, read mode, duplicate/missing/extra/order mismatch counts, screenshot path hash, and bounded error codes; it does not output raw thread ids, item ids, message text, task-card bodies, debug URLs, cookies, tokens, private routes, or logs. |
| `scripts/codex-mobile-pwa-shell-refresh-smoke.js` | Home AI live-debug visual smoke for PWA shell refresh readiness | Opens the embedded Codex Mobile shell through the live-debug lane, compares the visual harness client build id with `/api/public-config`, verifies boot recovery is hidden, app shell is visible, hard-refresh/page-refresh controls exist, shell refresh functions are callable, and service-worker/cache capabilities are observable. It is inspect-only by default and reports endpoint kind, expected build/cache ids, bounded booleans, screenshot path hash, and error codes without raw URLs, paths, DOM text, cookies, tokens, launch material, or logs. |
| `scripts/codex-mobile-long-turn-viewport-fixture.js` | Local Phase E headless Chrome fixture for single-thread mobile long-receipt viewport behavior | Uses bounded fake single-thread content and real `public/styles.css` to verify long final-receipt start anchoring, Usage visibility at bottom, shared/互斥 scroll-to-bottom and scroll-to-reply button slot, Composer/conversation separation, and non-overlap of the visible receipt/Usage bands. Reports only viewport, rect, scroll, boolean invariant, artifact path hash, and byte-count metadata; it does not read or output private threads, messages, task-card bodies, URLs, paths, cookies, tokens, screenshots, or logs. |
| `scripts/codex-mobile-phase-b-readback-smoke.js` | Local Phase B production-readback smoke for thread-list/detail diagnostics | Calls authenticated `/api/public-config`, `/api/threads`, and `/api/threads/:id?mode=recent`, waits briefly for startup thread-list prewarm to settle before the first list read, verifies `coldPathOwner` and thread-detail timing fields, optionally requires `projection-active-overlay`, follows a deferred thread-list response with bounded full-list and warm-check reads, classifies the active-overlay proof gate into bounded reason/next-action labels, and prints only metadata-safe build ids, hashes, read modes, owner/reason labels, prewarm status, source counters, app-server request-window and latency split/count fields, baseline final-filter/merge counters, counts, and timings. |
| `scripts/codex-mobile-thread-tile-visual-fixture.js` | Local Phase E headless Chrome fixture for wide-screen thread-tile layout, Composer keyboard stability, and task-card presentation | Uses the existing thread-tile layout/state helpers and real `public/styles.css` to generate bounded fake pane content, screenshot the wide/overlay/keyboard/task-card layouts, and verify pane/board/composer bounds, overflow split columns, hidden bottom buttons, operation bubble duration visibility, typed Composer containment, keyboard-open app transform stability, injected task-card collapsed/expanded bounds, bounded expanded scroll region, and non-overlap without reading private threads, messages, task-card bodies, uploads, paths, cookies, tokens, or logs. |
| `start-codex-desktop-shared.ps1` / `start-codex-desktop-shared-hidden.vbs` / `start-codex-desktop-*.cmd` | Windows Desktop shared-mux launcher and profile escape hatch | Use `-ProfileId default|current|previous` or `-CodexHome` so Desktop attaches to the same selected profile endpoint as Mobile Web; preserve non-default profile auth/config backup and shared thread/workspace state hardlink/junction setup. When no real Codex exe is explicit, prefer the newest installed OpenAI Codex bin before the older runtime copy. Desktop shortcuts should target the VBS wrapper or another hidden host, not the `.cmd` wrappers. Only the Desktop GUI may be visible; bridge helper processes must be windowless. |
| `start-codex-*-macos.sh` | macOS standalone/shared launch helpers | Keep shell syntax validated with `npm run check:macos`. |

## Adapter Services

| File | Responsibility |
| --- | --- |
| `adapters/active-turn-staleness-service.js` | Stale/superseded active-turn detection before existing-thread message submission. |
| `adapters/message-pending-echo-service.js` | Short-lived pending user-message echo injection for active-turn steering that is still waiting. |
| `adapters/message-input-service.js` | Upload-aware model input policy, image context mode, and extended-history persistence policy. |
| `adapters/continuation-handoff-compaction-service.js` | Workspace context compaction before rollout continuation, including `.agent-context/PROJECT_CONTEXT.md` and `.agent-context/HANDOFF.md` archive/rewrite/report behavior, archive ignore guard creation, and compact-handoff current-state guidance. |
| `adapters/turn-usage-summary-service.js` | Rollout `token_count` parsing and completed-turn context/token usage summary attachment. |
| `adapters/token-usage-stats-service.js` | Runtime SQLite token ledger for completed turns, keyed by `thread_id + turn_id`, with Workspace/day aggregation for sidebar stats, known Windows mojibake cwd normalization, replay-idempotent completed-turn writes that do not invalidate aggregate caches when the stored Usage row is unchanged, an in-process query cache for thread-list first-paint decoration that can reuse expired entries when the date/thread/workspace key still matches and no real Usage write has invalidated the cache, and a bounded compiled Workspace snapshot cache so repeated list paints do not rebuild aliases/cache keys for the same visible Workspace set. |
| `adapters/thread-goal-service.js` | Read-only `<CODEX_HOME>\goals_1.sqlite` goal lookup, status normalization, public thread goal shape, list/detail decoration, and pure continuation-goal migration planning. |
| `adapters/mobile-archive-index-service.js` | Runtime JSON archive tombstone index for Mobile-hidden thread ids, used when recovered/old-profile rows cannot be reliably hidden through app-server or SQLite state. |
| `adapters/public-pull-request-service.js` | Public GitHub pull request status normalization for prompt-only public PR checks. |
| `adapters/github-link-preview-service.js` | GitHub repository/issue/pull/commit URL parsing, REST API URL construction, and bounded preview payload normalization. |
| `adapters/push-notification-service.js` | Web Push turn tracking, sub-agent suppression classification, completed-turn notification thread-title resolution, and the bounded app-server display-summary cache used before SQLite fallback. The display-summary cache can return cached thread summaries without re-running expensive read-time decoration for thread-list merge paths that already carry request-scoped rollout metadata. |
| `adapters/shared-chain-restart-service.js` | Authenticated restart endpoint orchestration, including Windows hidden shared-chain restarts, the separate `SYSTEM` helper-task escape from the current scheduled-task process tree, and macOS LaunchAgent `kickstart` / system LaunchDaemon stdout/stderr pre-repair / one-shot fallback policy. |
| `adapters/generated-image-cache-service.js` | Caches app-server `imageView` screenshot files and `imageGeneration.savedPath` PNGs into runtime-owned generated-image storage before browser rendering. |
| `adapters/hermes-plugin-service.js` | Independent Hermes Mobile embedded-app plugin manifest, callback/origin registration, launch/session token policy, frame-ancestor metadata, and bounded launch/session appearance sync. |
| `adapters/hermes-notification-delegate-service.js` | Backend-only Hermes Action Inbox notification delegation, safe payload normalization, Hermes endpoint/key resolution, and response sanitization. |
| `adapters/home-ai-autonomous-delivery-return-service.js` | Backend-only Home AI Autonomous Delivery Loop return-card event client. It normalizes bounded terminal return metadata, posts to `/api/autonomous-delivery/return-card-events` through the trusted Home AI web-key path, and excludes card bodies, prompts, uploads, provider payloads, cookies, launch tokens, access keys, database rows, and logs. |
| `adapters/turn-completion-receipt-service.js` | Builds bounded completed-turn detail receipts for Hermes plugin Inbox/thread-message delegation from final assistant text plus usage summary. |
| `adapters/thread-task-card-service.js` | Cross-thread task-card normalization, readable visible-text guards, bounded reasoning-effort request metadata, JSON store persistence, idempotency, single/multi-target creation, source/target authorization, source-thread direct approval for thread-callable delegation and return-to-source cards, state transitions, approval in-flight persistence, manual return cards for pending/approved originals, terminal return-card observer events for Home AI delivery-loop state tracking, autonomous workflow grants, same-pair auto-approval, automatic completion return cards, runtime injection evidence, approval injection payload generation, and batch pending-count reads for thread-list state decoration. |
| `adapters/thread-task-card-routing-service.js` | Cross-thread task-card target reference parsing, exact id/title/workspace resolution, visible/deliverable target filtering, archived/hidden/subagent/sidecar rejection, same-cwd canonical selection, and public target metadata shaping. |
| `adapters/thread-list-fallback-baseline-service.js` | Pure thread-list fallback baseline builder. It owns state DB / rollout session / session-index fallback source collection, per-source timings/counts, source-order merge, result limiting, final-filter/merge work counters, whitelisted numeric source diagnostics, same-read temporary source context, and a memory-only visibility-scoped source snapshot below final-list cache keys. It reuses source snapshots across different `cwd/search/limit` final-list misses while preserving existing filter/merge/limit semantics, and it does not persist, prewarm, or change app-server authority. |
| `adapters/thread-list-fallback-cache-service.js` | Process-lifetime thread-list fallback cache policy, including stable cache-key construction from visible workspaces/projectless thread ids, compatible wider-limit cache hits for narrower same-scope requests, optional TTL expiry, cache-hit/miss/expired decision diagnostics with bounded cache-key hashes/build counters, cached baseline source timings/counts/source counters, source-snapshot diagnostics, and incremental status/title/archive updates without forcing a full state-db/rollout/session-index rebuild. Cold miss/expired baseline construction is delegated to `thread-list-fallback-baseline-service`. |
| `adapters/thread-list-fallback-prewarm-service.js` | Startup-only thread-list fallback prewarm policy. It schedules at most one delayed metadata-only default-list fallback read after the listener starts, warming the existing process-lifetime fallback cache/source snapshot without changing app-server authority, fallback merge/filter/limit semantics, persistence, or frontend refresh behavior. It can notify an internal completion hook with the already-read fallback rows so server glue can schedule active-window prewarm for active rows, while public lifecycle/readback status remains bounded to counts/timings and does not expose thread ids, titles, messages, paths, or logs. |
| `adapters/thread-list-app-server-fetch-policy-service.js` | Pure thread-list app-server request-window, initial-fallback attempt, initial-fallback metadata, and latency-attribution policy. It keeps cursor pages exact, preserves the legacy 500-row app-server window for workspace-filter and archived paths, bounds default/search overfetch to `max(limit * 2, 80)` capped at 500, classifies explicit `initial=warm-fallback` warm-cache/cold-baseline first paint separately from ordinary default warm-cache early return, counts bounded raw/visible/workspace-filtered app-server rows, and exports metadata-only timing fields including measured/unattributed app-server elapsed time for Phase B readback. Ordinary default warm-cache early return is controlled by `CODEX_MOBILE_THREAD_LIST_DEFAULT_WARM_FALLBACK` and does not build a cold fallback baseline on cache miss. |
| `adapters/thread-list-route-merge-service.js` | Pure `/api/threads` route merge policy. It combines app-server and fallback rows, can drop fallback rows whose thread id is already present in the authoritative app-server result, preserves original/effective input diagnostics, and delegates duplicate summary resolution to the summary merge service without copying private titles, messages, task-card bodies, uploads, cookies, or logs. |
| `adapters/thread-list-summary-merge-service.js` | Pure thread-list summary merge policy. It strips detail-only fields, applies archived/sub-agent filters, resolves true duplicate ids through display-summary merge, avoids the duplicate-display stage for unique ids, sorts/limits summaries, and emits bounded merge-stage timings for warm-cache versus duplicate-pressure attribution. |
| `adapters/thread-list-response-coalescer-service.js` | Default thread-list in-flight response coalescing policy. It shares one concurrently generated default full-list `/api/threads` response across identical follower requests, avoiding repeated app-server `thread/list`, merge, and decoration work during burst refreshes. It is disabled for cursor, search, workspace, archived, fallback-defer, and warm-initial requests and adds only bounded coalescing diagnostics. |
| `adapters/thread-list-cold-path-diagnosis-service.js` | Pure thread-list cold/warm path attribution policy. It maps existing bounded thread-list timings into `coldPathOwner` / `coldPathReason` for warm fallback cache reuse, source snapshot reuse, deferred fallback, miss rebuilds, TTL-expired rebuilds, baseline dominant source attribution, final-filter work, merge dedupe work, limit-drop work, thread-list state attach, token-usage decoration, app-server-only reads, and app-server-error fallback without copying private thread content or changing list behavior. |
| `adapters/thread-list-summary-service.js` | Thread-list summary authority boundary. Strips detail-only fields such as `turns`, runtime settings, pending server requests, projection metadata, visible item keys, and detail-loaded flags before app-server/fallback list rows can become public summaries. |
| `adapters/phase-b-readback-decision-service.js` | Pure Phase B readback decision policy. It maps bounded readback smoke results into a root-cause next action such as active-overlay gate repair, projection-cache lifecycle, projection-input availability, thread-list fallback prewarm lifecycle, thread-list fallback baseline, final-filter work, fallback merge/dedupe pressure, limit-window review, cache freshness, app-server RPC/list latency, local list filtering/post-process latency, or ready/observe; its evidence includes bounded prewarm status, app-server request-window and latency split/count fields, and final-filter/merge/limit counters without copying private titles, messages, card bodies, uploads, paths, cookies, or logs. |
| `adapters/thread-turn-compaction-policy-service.js` | Pure server-side thread-detail compaction policy for operation retention, receipt-only item selection, ended/visible turn discovery, preserving diagnostic receipts such as empty-completion runtime diagnostics, and choosing which turns keep full operation detail. |
| `adapters/thread-completion-diagnostic-service.js` | Completed-turn runtime diagnostic policy for explicit empty final assistant messages. It reads bounded rollout completion evidence, builds `turnDiagnostic` / `runtime_completed_without_response` items, and appends them to completed turns without fabricating assistant receipts. |
| `adapters/thread-side-chat-service.js` | Server-side current-thread side-chat persistence under the Codex Mobile runtime root, including per-profile/thread isolation, transcript/draft/candidate/queue state, bounds, idempotency, cancellation, clear behavior, and safe public state shape. |
| `adapters/chatgpt-pro-planner-service.js` | Runtime ChatGPT Pro planner artifact store, artifact type/action normalization, bounded visible-workspace documentation reads, bounded thread-context projection, and safe public artifact shapes. |
| `adapters/chatgpt-pro-mcp-service.js` | Restricted ChatGPT Pro MCP connector JSON-RPC surface, separate runtime-only bearer-token authorization, tool schema projection, planner-tool dispatch, pending-default task-card delegation, and explicit direct-delegation gating. |
| `adapters/codex-mobile-mcp-config-service.js` | Per-Codex-Home `config.toml` registration for the Codex Mobile MCP stdio server | Appends `[mcp_servers.codex_mobile]` only when missing and writes command/script/server/key-file paths, not raw keys. |
| `adapters/workspace-source-write-guard-service.js` | Dynamic source-tree write guard for `跨工作区委派`: classifies app-server approval requests produced by the real workspace sandbox, allows current-workspace/MCP/read execution and narrow Home AI provided tool commands, and denies direct writes into other known source roots. |
| `adapters/thread-detail-projection-service.js` | Runtime Mobile thread-detail projection index and warm cache for large rollout threads. It is seeded from compact detail reads, updated from raw app-server notifications including intermediate deltas, treats `turn/completed` as an item-preserving patch, keeps one Usage summary per turn, keeps cache signatures tied to rollout size/mtime, summary state, and projection policy, exposes bounded lookup miss reasons, persists full non-partial dynamic projections with refreshed rollout stats, and owns signed partial `recent-window` / `turns-list-window` projections that are returned only with explicit `allowPartial`, are not accepted as full-cache authority, cannot overwrite a reusable full cache, and can be restored after restart to avoid a first-open `turns/list` read when the backing signature still matches. Cursor-backed `turns-list*` windows and legacy disk entries that persisted such windows as full cache are normalized as partial projection state rather than deleted. Notification-only partial shells without a projection signature are still rejected for detail reads so a background status patch cannot render as an empty current thread; the only supported read from those shells is the clone-only active-overlay snapshot API used by the proof-gated provider. Live notification shells now retain and clear `thread.activeTurnId` across active/completed events so the provider can infer the current active turn when summaries only expose active status. Normal projection lookup treats rollout size/mtime and policy changes as content-backing invalidation; summary timestamp-only changes no longer force `dynamic-summary-stale` when the backing signature is unchanged. Recent-mode callers may explicitly request a stale partial first-paint when only a signed partial window is available after backing movement; that result is marked `stalePartial` / `staleReason` and must be paired with a background refresh rather than treated as full-cache authority. Only an explicit active-overlay lookup with `allowPartial=true`, `activeOverlay=true`, active summary status, and `omitActiveTurnId` may reuse a dynamic or stale full projection as a history-only active-window input for the downstream proof gate. The service keeps a process-local full history baseline and restores persisted full cache before applying active notifications after restart, while still refusing to persist notification-only partial state as normal detail authority. That active-overlay lookup clones the cached projection window without the currently growing live turn, while the proof-gated provider supplies and compacts that live turn separately. |
| `adapters/thread-detail-summary-service.js` | Thread-detail summary resolver for `/api/threads/:id`, preserving the state-db -> started-cache -> rollout-session -> display-cache -> app-server lookup order for missing summaries, merging the process display-summary cache for existing local summaries, suppressing repeated synchronous app-server summary refreshes with bounded skip diagnostics, applying local-active overlay state, and emitting bounded summary diagnostics before detail projection/read logic runs. |
| `adapters/thread-detail-performance-service.js` | Bounded thread-detail timing diagnostics and cold/warm read-mode phase classification for `/api/threads/:id` responses. It records metadata such as summary/projection/thread-read/turns-list/large-window/prepare durations, read decision, projection hit/miss/unavailable state, bounded projection miss reason, projection source/version/cache age, projection seed status/source, active full-read requirement/reason, active-overlay window-first hot-path use, cold-path owner/reason, and partial projection phase classification without copying message bodies into diagnostics. Phase classification is driven by `readDecision`, `projectionState`, `projectionSource`, and `projectionSeedStatus` when `readMode` is sparse, so server readback aligns with client first-paint performance phases. |
| `adapters/thread-detail-bounded-read-policy-service.js` | Pure large-session bounded-read gate for thread detail reads. It owns the `CODEX_MOBILE_THREAD_DETAIL_TURNS_LIST_FIRST_BYTES` comparison semantics, projection rollout-size priority over summary rollout-size fallback, decision source/reason metadata, and disabled/no-size behavior before the orchestration service chooses `turns-list-large` versus full `thread/read`. |
| `adapters/thread-detail-active-read-policy-service.js` | Pure active-thread detail read policy. It normalizes active/running status evidence, derives bounded full-read reasons, decides whether recent partial projection / initial turns-list is allowed, and suppresses large-session bounded-read shortcuts for active summaries until a future active-window overlay proves it can preserve live/intermediate items without full `thread/read`. |
| `adapters/thread-detail-active-window-overlay-policy-service.js` | Pure active-window overlay eligibility and merge policy. It classifies bounded active overlay evidence, including MCP and dynamic tool calls as operation evidence, requires active turn id/projection window/authoritative source/matching turn plus operation, upload, explicit assistant-delta freshness, and receipt coverage, and fails closed to full `thread/read` whenever evidence is unknown, stale, mismatched, non-authoritative, or empty. Active-window history projections such as `turns-list-active-overlay-window` intentionally omit the live active turn, so their projection revision is not used to mark the live overlay assistant delta stale; ordinary projections still keep the stale-delta fail-closed gate. It also owns the pure projection-window + active-overlay-turn merge used by the orchestration seam after the proof gate passes; callers can inject a compactor so raw overlay turns are reduced before cloning into the detail response. |
| `adapters/thread-detail-active-overlay-provider-service.js` | Server-owned active overlay provider for read orchestration. It converts the projection service's memory-only notification snapshot into bounded policy evidence, including active turn id, clone-only overlay turn, coverage counts, v4 overlay/projection revisions, and timestamp metadata. It can derive the active turn id from the live projection snapshot when the summary only proves active status. It caches bounded active-turn evidence counts by thread, active turn, item count, and first/last item identity so repeated detail reads do not rescan a large live turn; the cache stores counts only, not message text, tool payloads, uploads, or private content. It does not merge, render, retry, or promote notification shells; missing snapshot, missing active turn, incomplete coverage, or missing assistant freshness evidence remains a full `thread/read` path. |
| `adapters/thread-detail-active-window-prewarm-service.js` | Server-side active projection-window prewarm coordinator. It schedules and deduplicates background `turns-list-active-overlay-window` reads for active/running threads after turn/status notifications or thread-list refreshes, reuses the same projection-window lookup and partial projection seeding path as `/api/threads/:id`, uses overlay revision/timestamp metadata when live overlay evidence is already available, but can preseed the history window before overlay turn evidence exists. It skips non-active summaries and already-cached windows, stores only bounded status metadata, and reduces the chance that the first user-visible detail open has to synchronously build an active overlay window from a large session file. |
| `adapters/thread-detail-active-window-read-coalescer-service.js` | In-flight read coalescer for active projection-window construction. It shares concurrent `turns-list-active-overlay-window` reads for the same thread between background prewarm and the foreground detail route, logs bounded `turns_list_coalesced` metadata for joiners, clears failed reads so retries can run, and intentionally does not coalesce other turns-list modes or other threads. It stores only promise lifecycle counters in process memory and never persists or copies message text, task-card bodies, uploads, tool payloads, or rollout contents. |
| `adapters/thread-detail-cold-path-diagnosis-service.js` | Pure thread-detail cold-path attribution policy. It maps existing bounded timing/read fields into `coldPathOwner` / `coldPathReason` for warm projection, active full-read policy, projection-cache misses/seeding, missing projection input, summary-sourced large windows, app-server `thread/read`, app-server `thread/turns/list`, and summary fallback without changing read strategy or copying private thread content. |
| `adapters/thread-detail-read-orchestration-service.js` | Service-level coordinator for `/api/threads/:id` read phases. It owns the summary -> hidden-check -> projection lookup/hit/miss reason -> optional active-overlay proof seam -> recent turns-list -> large-rollout bounded turns-list -> full `thread/read` -> turns-list fallback -> summary fallback ordering, bounded phase timings, response-mode decisions, `mode=recent` partial projection allowance, stale-partial first-paint scheduling, and partial recent-window seeding while `server.js` supplies transport-specific read/prepare functions. Active/running summaries still fail closed to full `thread/read` unless the injected server-owned active-overlay provider plus `thread-detail-active-window-overlay-policy-service` proves complete coverage; ordinary projection hits for active summaries are treated as proof-gate window candidates rather than final responses. When a dedicated `activeOverlayProjectionWindowLookup` is injected, active summaries use that lightweight projection-window hot path before ordinary projection lookup, so the route avoids a duplicate normal projection normalize/assemble pass before the proof gate. The active-overlay provider may return synchronously, and orchestration only awaits promise-like provider results so a sync proof lookup does not yield the event loop and inflate active-detail latency. When recent mode gets a stale signed partial first-paint, it returns `projection-stale-partial-hit` and asks the server to refresh the current window in the background instead of blocking first paint on app-server `turns/list`. When the provider has live active-turn evidence but the active-overlay projection-window lookup lacks a window, orchestration retries or builds a `turns-list-active-overlay-window` skeleton, seeds it as a partial projection window for subsequent reads, and re-runs the same proof gate. After the proof gate passes, orchestration injects the server-owned active-overlay turn compactor before merge so raw tool arguments/results and long operation payloads do not enter the public detail response. Provider-missing or incomplete evidence remains full-read and is reported through bounded active-overlay diagnostics. |
| `adapters/thread-detail-response-budget-service.js` | Pure thread-detail response budget policy. It trims operation, reasoning, and intermediate assistant/plan item tails after projection/read orchestration but before the HTTP detail response is sent, then rebuilds v4 visible keys through `thread-visible-item-normalizer` so smaller responses do not create projection signature mismatches. It owns completed-turn receipt-only assistant retention, active-turn recent assistant retention, stale active-looking turn downgrading when the thread has a current active turn id, item-count and byte-pressure triggered progressive active operation/reasoning/assistant limits, progressive active retained-text preview budgets with `mobileActiveTextBudget`, progressive active user-input previews with `mobileUserInputBudget` for long task-card/bootstrap text and inline `data:image` input parts, progressive active operation-payload previews with `mobileOperationPayloadBudget` for command output, operation display text, file-change lists, tool arguments/results/content items, and bounded action/request/response payloads, pressure-triggered first-paint visible-item ceilings that prune older operation/reasoning items while protecting user/image/Usage/final receipt rows, second-stage first-paint byte ceilings that preview non-current or historical completed assistant/reasoning receipts with `mobileFirstPaintTextBudget` when item budgets still leave an oversized detail body while protecting the latest completed turn in resting detail, active/completed operation retention including `collabAgentToolCall`, reasoning retention, and bounded `mobileDetailResponseBudget` evidence. |
| `adapters/thread-detail-route-service.js` | Testable `/api/threads/:id` detail route coordinator. It maps URL `mode=recent` to `preferRecentTurns`, constructs bounded per-request `threadLog` metadata, calls read orchestration, sends the JSON response, and preserves the `complete=false` logging contract while `server.js` keeps only HTTP matching/lifecycle glue. |
| `adapters/thread-detail-projection-input-service.js` | Pure projection cache-signature input builder for thread detail reads, including rollout path aliasing, rollout stats injection, retained-turn window, summary update time, and summary status. |
| `adapters/thread-detail-projection-result-service.js` | Pure projection-hit result assembler for thread detail reads. It merges cached projection output with display summary data, applies session-index title hydration, runtime model/effort decoration, live-status normalization, public runtime settings, and projection read-mode metadata including partial source/kind before the route sends the detail response. Response-ready v4 hits with complete thread/turn/item visible-key metadata skip the raw `compactThreadReadResult` pass; invalid, legacy, oversized, or incomplete projections still fail closed to the existing compaction path. Ordinary projection hits still require any local active turn to already be present in the cached window; only the `activeOverlay` assembly path may accept a partial window that lacks the active turn because the live overlay provider supplies and proof-gates that turn before response. |
| `adapters/thread-visible-item-normalizer.js` | v4 pure visible-item normalizer for app-server detail reads, bounded turn pages, raw notifications, rollout metadata, and synthetic diagnostics. It emits stable `mobileVisibleKey` / `mobileVisibleKind` metadata, classifies command/file/tool/MCP/collab-agent calls as `operation`, and emits explicit context-compaction notices only from explicit notification state. See `docs/THREAD_DETAIL_PROJECTION_V4_DESIGN.md`. |
| `adapters/thread-detail-projection-v4-service.js` | v4 canonical thread-detail projection service with stable visible keys, revision tracking, completion patching, pending-compatible output, bounded diff helpers, and transparent forwarding of partial recent-window seed/get/lookup options to the base cache. It wraps the v3 cache/patch substrate but exposes v4 normalized output and bounded lookup miss reasons by default; v4 also normalizes active-overlay snapshots, caches unchanged normalized overlays by active turn/revision/timestamp, and attaches monotonic overlay revisions so assistant-delta freshness can be proven without reading full threads. Ordinary cache/dynamic hits can reuse already-normalized visible metadata by refreshing revision/source/read-mode and aggregate visible-key lists instead of deep re-normalizing every turn/item; if any retained item lacks v4 visible metadata or has duplicate visible keys, the service falls back to full normalization. Active-overlay window lookup can request `skipNormalizeResult` because the cached partial window was already normalized when seeded; that hot path only reattaches bounded v4 projection metadata and avoids re-normalizing the live overlay turn. v3 remains available through `CODEX_MOBILE_THREAD_DETAIL_PROJECTION_V4=0`. |
| `adapters/workspace-registry-service.js` | Mobile Web-created workspace folder validation, runtime registry persistence, allowed-root policy, canonical realpath handling, optional Codex Desktop global-state workspace-root sync, and public workspace shape. |
| `adapters/codex-profile-service.js` | Single-active Codex profile discovery, safe `auth.json` account display, account-scoped profile quota snapshots with shared-rollout protection, active-profile persistence, and switch eligibility policy. |
| `adapters/sqlite-cli.js` | Cross-environment `sqlite3` discovery and JSON result execution. |

Add new service modules when logic has independent inputs/outputs, state rules, route policies, or tests. Keep route handlers thin and make service behavior directly testable.

## Public Frontend Files

| File | Responsibility |
| --- | --- |
| `public/app.js` | Main app state, rendering, thread list/detail including older-turn pagination for fallback detail windows, empty-detail/history-evidence mismatch diagnostics and detail refresh, refresh render fact collection that supplies mounted signature/DOM facts to the plan layer, thread-tile board expected/rendered turn counts that feed the same conversation DOM authority path, thread-open watchdog slow-path diagnostic reporting for long pending-but-eventually-successful loads, read-only thread goal display and SSE updates, composer `/g` goal dialog submission/action handling through the goal routes with immediate submitted-goal card display, unified composer `@` intent menu/dialog for Goal, task cards, autonomous collaboration, and ChatGPT Pro while preserving legacy prefixes, Push UI, update/restart UI, continuation UI, settings-panel Codex profile display/switching, composer runtime Fast-dot/model/effort/permission/quota controls including the hidden Codex Fast service-tier toggle, source-side `#` task-card auto-send orchestration with stable draft settlement, queued draft lookup across long threads, silent in-conversation `creating` state, target-side-only pending task-card rendering with on-demand task-card body loading, explicit runtime diagnostic item rendering, runtime application of plugin session appearance, silent Hermes embed EventSource fallback recovery, thread-list load-policy execution for warm fallback first paint, and Hermes embed iframe-owned back/edge-swipe behavior. |
| `public/styles.css` | Full app shell, mobile/tablet layout, operation cards, composer, sidebar, modals, PWA-safe layout. |
| `public/sw.js` | Service worker, app-shell cache, Web Push notification click handling. |
| `public/api-client.js` | Authenticated fetch helper. |
| `public/runtime-settings.js` | Model/effort/permission option normalization and labels. |
| `public/draft-store.js` | Browser-local draft persistence, runtime-only next-turn model/effort/permission draft state, and resumable attachment draft data. |
| `public/markdown-renderer.js` | Markdown rendering rules for conversation/source text. |
| `public/viewport-metrics.js` | Visual viewport and keyboard-shrink helpers. |
| `public/conversation-scroll.js` | Scroll position and bottom-follow helpers, including near-bottom metrics, submitted-message/viewport follow leases, bottom-follow lease evaluation, bottom-follow retry schedule planning, user-reading-current-turn planning, auto-scroll hold update planning, local-patch scroll completion planning, single-thread full-render bottom-follow planning, and mutually exclusive bottom-jump versus receipt-jump button visibility planning. |
| `public/image-compressor.js` | Browser-side image compression before upload. |
| `public/plugin-embed.js` | Hermes iframe embed-mode helper for launch detection, bounded appearance/query parsing, notification/task-card route-hint open/focus/scrub policy, navigation/back eligibility messages, back messages, and internal-window policy. |
| `public/plugin-voice-input.js` | Home AI embedded-plugin voice-input protocol helper for `voice_input.*` capability, insert-result, start/stop, and commit-result messages. It is inert for standalone Codex Mobile Web unless the page is running in `/?embed=hermes`. |
| `public/home-ai-diagnostic-reporting.js` | Home AI diagnostic report policy helper for repeated-failure thresholding, slow-path stable repeat aggregation, success-based counter clearing, throttling, bounded payload sanitization, short-hash helpers, and safe `homeai.diagnostic.report` parent-frame posting. It owns plugin-side evidence shaping for task-card/session failures, repeated slow-success detail/list paths, and repeated projection/DOM mismatches only; Home AI owns case dedupe, Owner notification, and Owner-triggered repair-card dispatch. |
| `public/thread-diagnostic-events.js` | Pure Codex thread diagnostic snapshot, outcome-effect, and event payload planner for product-critical projection/render/session reports. It owns tile/single/transition projection snapshot planning, projection-consistency failure/success effect planning, thread-detail response diagnostic failure/success effect planning, plus bounded `render_signature_mismatch`, `duplicate_render_keys`, `turn_order_mismatch`, `primary_shell_selection_conflict`, `empty_visible_detail_mismatch`, `detail_patch_rejected`, `thread_detail_load_failed`, `thread_detail_refresh_failed`, `thread_detail_slow_path`, `thread_list_slow_path`, and `thread_detail_response_contract_mismatch` payload construction, including render-plan reason, patch-reject reason, Primary-shell selection evidence, empty-state/detail-evidence mismatch counts, visible-count deltas, DOM/duplicate/pane counts, turn-order/latest-turn/missing-DOM-turn mismatch counts, load/refresh status/duration/thread hash metadata, detail read-mode/phase/projection/cursor/count/cold-path metadata, thread-list fallback/app-server timing metadata, and breadcrumbs without message bodies, task-card bodies, uploads, private paths, cookies, tokens, or long logs. |
| `public/thread-status-hints.js` | Pure thread-list status freshness policy for running hints, unread markers, viewed timestamps, submitted-processing hints, and mux replay event-time handling. |
| `public/thread-performance-metrics.js` | Pure client-side performance-event field extraction for thread-list and thread-detail diagnostics, including `serverTimings`, cold/warm `performancePhase` classification for detail reads from bounded `readDecision` / `readMode` / projection fields, thread-list fallback-cache decision phase mapping, bounded `detailShape` count summaries, `thread_detail_first_paint` / `thread_refresh_ms` / `thread_detail_full_ready` payload construction, detail and thread-list slow-path diagnostic planning with cold-path owner/reason and bounded list app-server/fallback timing passthrough, and detail-response contract planning for empty projection shells, active-thread window downgrades, and projection windows incorrectly marked full without message bodies, task-card bodies, uploads, private paths, cookies, tokens, or long logs. |
| `public/thread-list-load-policy.js` | Pure client-side thread-list request policy. It selects `initial=warm-fallback` for ordinary first list paint when no search/workspace filter is active, preserves the active-detail `fallback=defer` path, and leaves search/workspace/explicit no-defer requests on the authoritative full path. |
| `public/thread-list-stable-order.js` | Pure client-side thread-list order-stability policy. It keeps existing default-list rows in their prior visible order for a bounded short hold window during background refreshes while still inserting new rows by server rank and resetting immediately for search/workspace scope changes, so status/time data can update without second-level list reordering under the user's pointer. |
| `public/live-operation-dock-state.js` | Pure mobile operation dock state/content/template policy for live-turn compact bubble minimum dwell, expanded pinned sheet preservation, same-thread live recall visibility, compact/expanded mode normalization, operation card content planning such as class tokens/title/detail/status/duration visibility/empty-detail state, and final operation card HTML template generation with injected escaping and bounded duration data-attribute filtering. |
| `public/thread-detail-state.js` | Pure thread-detail item merge/state policy for visible-field preservation, visible-text render identity, completed-receipt text retention, context-compaction notice cleanup, operation metadata retention, authoritative completed-receipt detection, local-only item retention/drop rules, live-to-completed same-turn visible-item preservation during browser refresh merges, and thread-list-summary versus loaded-detail ownership rules. It strips detail-only fields before rows enter the thread list, treats an empty `turns: []` object as loaded detail only when the client-owned `mobileDetailLoaded` marker was set by a successful detail API path, separates loaded detail from reusable loaded detail so `cached-current` can only reuse nonempty detail turns, plans thread-open cache reuse including empty cached-detail diagnostic intent, plans thread-open loading shells from id-matched summaries without accepting stale detail-only fields, plans summary merges without preserving stale detail state, plans summary-only current-thread recovery into a loading shell plus bounded refresh/diagnostic intent, plans summary-only recovery effect ordering for state replacement, bounded client events, and refresh scheduling, plans empty-detail/history-evidence recovery signatures, cooldown reasons, and bounded client event fields, and owns recent detail render evidence construction/freshness/thread matching/nonempty proof for projection diagnostics. |
| `public/thread-detail-render-plan.js` | Pure thread-detail refresh and single-thread shell render planning policy. Given current refresh inputs, it plans whether to refresh, snapshots thread id/load sequence, normalizes recent/full mode, selects API query/timeout, and records active-controller abort intent while leaving the real abort/network side effects in app code. Given refresh response identity, it plans stale-response rejection plus ordered detail-loaded, render-evidence, and current-thread merge effects while leaving real state mutation in app code. Given previous, next, and currently rendered conversation signatures, it owns refresh render input field selection and decides whether a refresh is metadata-only, eligible for local DOM patch, or must use full render. It owns refresh patch surface planning and stage composition: whether tile-pane DOM state should be probed, the DOM-probe effect intent, and whether the final patch path is tile/single surface based on tile mode, tile conversation surface, and the probed DOM patch surface. It also owns refresh patch execution planning and stage composition: whether to try tile-pane patch, whether single-thread local patch is eligible, whether metadata-only tile misses update metadata without full render, which fallback action applies, and how execution plans compose into ordered patch-attempt effects. It owns refresh patch-attempt effect ordering, patch attempt aggregation state/context/reducer, refresh patch attempt result planning and result-stage composition, tile-pane terminal success, local-patch success/rejection, metadata-only tile miss, finalized patch result shape, patch telemetry (`detailPatchMs`, `patchTimingSource`, normalized rejection reason), local rejection visible-shape request/evidence-effect planning, local rejection evidence resolution, local rejection diagnostic input planning, and local rejection diagnostic effect selection. It owns refresh outcome execution planning and stage composition: metadata update mode, ordered metadata effect list, explicit execution action/timing target, execution-effect entries for metadata/full-render/no-op, full-render execution, projection-consistency check planning and effect selection, plus the fixed composition from `renderPlan` + `patchAttemptResult` to `renderOutcome`, `executionPlan`, execution effects, and consistency effects. It owns refresh performance input assembly, refresh reporting stage composition, refresh telemetry effect ordering, refresh failure diagnostic effect selection, completion side-effect planning, first-paint response effect ordering, full-backfill response effect ordering, first-paint performance input selection, full-backfill performance input selection, first-paint pre-render local-state effect ordering, first-paint draft-restore effect ordering, first-paint after-render effect ordering, first-paint post-timing effect ordering, first-paint post-render effect ordering, cached-current post-render effect ordering, loading-shell post-state effect ordering, load-error render effect ordering, full-backfill post-render effect ordering, API first-paint telemetry effect ordering, cached-current telemetry effect ordering, full-backfill telemetry effect ordering, thread switch start/cancel/error client-event payload planning, and post-merge effect ordering for finalized render outcomes, cached-current detail reuse, first-paint detail completion, and full-backfill detail completion. It also plans single-thread early-shell execution for loading and load-error terminal renders, single-thread full-render shell modes for loading, load-error retry, detail content ordering, and empty/read-warning states, single-thread shell conversation update inputs for `updateConversationHtml()` including the explicit post-render projection-consistency check flag, single-thread shell post-update effect ordering, plus workflow-dominated recent-window history auto-backfill decisions and effect ordering when older cursors prove prior turns are available. It keeps refresh request/mode/abort planning, refresh render input field selection, patch surface selection and stage composition, patch-surface probe effect selection, stale-DOM patch eligibility, tile/single patch execution policy, patch attempt effect ordering, patch attempt aggregation, patch attempt result interpretation, refresh patch telemetry selection, local patch rejection visible-shape evidence-effect planning, local patch rejection evidence resolution, local patch rejection diagnostic input/effect selection, refresh failure diagnostic effect selection, refresh outcome execution policy and stage composition, refresh metadata effect selection, execution-action branching, consistency-check branching and effect selection, performance-input field selection, refresh reporting stage composition, telemetry side-effect ordering, completion-effect selection, first-paint response effect ordering, full-backfill response effect ordering, first-paint performance input selection, full-backfill performance input selection, first-paint pre-render local-state effect ordering, first-paint draft-restore effect ordering, first-paint after-render effect ordering, first-paint post-timing effect ordering, first-paint post-render effect ordering, cached-current post-render effect ordering, loading-shell post-state effect ordering, load-error render effect ordering, full-backfill post-render effect ordering, API first-paint telemetry effect ordering, cached-current telemetry effect ordering, full-backfill telemetry effect ordering, thread switch start/cancel/error event payload selection, post-merge effect ordering, early-shell branching, history auto-backfill classification/effect ordering, full-render shell selection, shell update input selection, and shell post-update effect selection out of `public/app.js` and is covered by focused tests. |
| `public/thread-detail-merge-state.js` | Pure thread/turn-level thread-detail merge orchestration. It coordinates v4 projection delegation, incoming turn merge, stale mobile load flag cleanup, active live-turn retention, expanded-history preservation, initial-submission echo cleanup, and merge authority that prevents an empty incoming detail window from erasing an existing stronger visible detail state while `public/app.js` supplies item-level merge functions and runtime active-turn state. |
| `public/thread-detail-v4-merge-state.js` | Pure v4 projection merge policy. It detects v4 projection threads, preserves pending optimistic user-message overlays until durable projection echoes arrive, keeps active/regressive visible projection turns when the incoming window is weaker, preserves newer projection revisions during regressive refreshes, and enforces the empty-incoming visible-detail authority rule for `projection-v4-dynamic` responses. |
| `public/thread-detail-patch-plan.js` | Pure patch planning for thread-detail local refreshes. It verifies shape-preserving visible-item updates, produces reuse/patch/insert operations, classifies tile/single DOM patch surfaces, owns local patch preflight rejection policy, owns tile-pane terminal classification, and plans turn-level item-patch/insert-turn/replace-turn operations before browser patch work starts. Keep real DOM lookup/mutation in `public/thread-detail-dom-patch.js` and app-specific render callbacks/signature calculation in `public/app.js`. |
| `public/thread-detail-dom-patch.js` | DOM patch executor/planner for thread-detail refresh plans. It applies visible-item reuse/patch/insert operations, visible item insert anchoring, live text item render-key lookup / HTML patch sequencing, turn-level item-patch/insert-turn/replace-turn operations, turn article insertion anchoring, turn article render-key lookup, rendered turn article creation, thread-detail surface hydration orchestration, conversation HTML update planning for stable/changed signatures, conversation HTML update effect planning for root hydration, rendered conversation signature writeback, patch-shell signature writeback, and scroll/bottom-button scheduling, expected-visible-turn versus rendered-DOM-turn authority checks and bounded DOM-authority invalidation payload planning, local DOM patch completion snapshot normalization for tile-pane terminal state / single-thread facts / signatures / scroll action, local DOM patch completion planning for tile/single completion, local completion effect planning for root hydration, rendered conversation signature writeback, patch-shell signature writeback, and scroll/bottom-button scheduling, patch-shell signature state, hydrate options, scroll action, explicit completion snapshots, and successful-patch transaction ordering with commit/post-commit stages before running app-owned dock/bind effects. It also owns keyed child DOM reconciliation (`patchNode` / `patchChildNodes` / `patchHtml`) with injected DOM lookup/render/patch/hydration callbacks, returning structured bounded patch results and failure reasons so `public/app.js` no longer owns those operation loops, visible item anchor fallback, lookup selector policy, live text patch sequencing, turn article creation step, hydration callback sequence, conversation update branching, stable-signature DOM-empty invalidation, conversation update post-effect branching, local patch completion snapshot normalization, local patch completion branching, local completion post-effect branching, keyed child patch algorithm, success-effect transaction gating, or global scratch storage for patch rejection reasons. |
| `public/thread-detail-actions.js` | Thread-detail click action recognition policy. It identifies previewable images, rich content controls, approval answers, task-card controls, task-card drafts, and server-response controls with root containment and stable action plan fields, while `public/app.js` still owns event listener wiring and business execution. |
| `public/thread-tile-actions.js` | Thread-tile interaction recognition policy. It classifies pane selection, switch-menu actions, pane-count/close controls, bottom jump, operation toggles, scroll targets, and drag/drop targets with root containment and stable action plan fields, while `public/app.js` still owns event listener wiring and business execution. |
| `public/thread-tile-state.js` | Thread-tile pane-state policy. It normalizes pane count, computes automatic/effective/min/max pane-count state from layout capacity, candidate ids, running ids, and explicit user count, plans pane display layout capacity/visible panes/columns/rows/column-groups for overflow and explicit split pairs, owns pane render signature schema planning, owns pane render frame scheduling and patch-miss full-render policy, owns pane-local patch preflight/failure-reason classification, owns pane-local patch completion side-effect planning, owns pane detail-load concurrency limit planning, owns shared Composer target/placeholder planning for tile selected-pane versus current-thread/new-thread fallback, owns Composer draft runtime restore planning for Fast/model/effort/permission when pane targets switch, owns pane-local scroll metrics/hold/bottom-button/restore-distance planning, owns thread-tile viewport baseline and Composer chrome-height planning for keyboard-active split-screen layouts, normalizes pinned thread ids, selected pane fallback, split-pair updates, display-settings payload/application/load-migration planning, active-id sync, active pane sync planning, candidate pane id planning, switch-menu option/control planning, explicit selected-pane action and side-effect planning, pane slot mutation side-effect planning including pane count/close effects and patch-miss full-render escalation intent, pane-local operation bubble dwell/expiry/mode/signature/minimum-refresh rules, operation mode toggle planning, operation dock render-branch planning, refresh timer planning, refresh target selection, detail-load queue/abort/defer planning, detail-load queue-drain scheduling, detail-load skip/background/loading decisions, detail-load lifecycle side-effect planning, pane slot mutation planning for thread replacement, drag reorder, up/down split, thread-list-open replacement, drop-zone intent, and pane count/close planning while `public/app.js` still owns DOM/render/timer/network/detail-load execution. |
| `public/thread-tile-layout.js` | Pure wide-screen thread tile layout policy for viewport/sidebar/orientation to physical columns/rows, recommended pane capacity, user pane safety ceiling decisions, and column-group packing for overflow/split panes. It uses browser CSS viewport width, keeps iPad portrait in single-thread mode, keeps tablet landscape conservative, lets desktop physical columns grow beyond four when width allows, and groups overflow panes inside existing columns so only the target column becomes an up/down split. Manual `threadDisplay.paneCount` is passed in so the helper can derive a tighter desktop pane width for explicitly requested panes without changing automatic mode. The actual visible pane count is now computed by `public/thread-tile-state.js` from layout capacity plus current/running-thread facts; drag-created `paneSplitPairs` remain owned by server-saved `threadDisplay` settings and pane-state policy. |
| `public/build-refresh-policy.js` | Pure app-shell build comparison policy so browser refresh prompts only fire for recoverable newer-server shell changes. |

`public/app.js` is large. For new frontend behavior, first check whether the change belongs in an existing helper module or can be extracted into a new public helper with focused tests.
Current render-only exceptions include the first-paint visible budget notice:
`public/app.js` renders `mobileVisibleItemBudget` / `mobileOmittedVisibleItemCount`
as a compact non-`data-item` notice in single-thread and tiled panes, and adds
the bounded budget summary to the conversation signature so server-budgeted
omissions are not diagnosed as client-side lost messages. The budget policy
itself remains in `adapters/thread-detail-response-budget-service.js`.

## Test Map

| Area | Tests |
| --- | --- |
| app-server/mux protocol | `test/protocol.test.js`, `test/desktop-profile-launcher.test.js`; include Desktop shim no-window coverage when changing `codex-app-server-mux-shim.cs` |
| active-turn send behavior | `test/active-turn-staleness-service.test.js`, `test/new-thread-route.test.js`, `test/message-pending-echo-service.test.js` |
| workspace list/create | `test/workspace-registry-service.test.js`, `test/new-thread-route.test.js`, `test/mobile-viewport.test.js` |
| conversation rendering and operation cards | `test/conversation-render.test.js`, `test/collab-agent-render.test.js`, `test/live-operation-dock-state.test.js`, `test/thread-detail-state.test.js`, `test/thread-detail-render-plan.test.js`, `test/thread-detail-merge-state.test.js`, `test/thread-detail-v4-merge-state.test.js`, `test/thread-detail-patch-plan.test.js`, `test/thread-detail-dom-patch.test.js`, `test/thread-detail-actions.test.js`, `test/thread-tile-actions.test.js`, `test/thread-tile-state.test.js`, `test/thread-completion-diagnostic-service.test.js`, `test/thread-item-timestamp-enrichment.test.js`, `test/thread-turn-compaction-policy-service.test.js`, `test/thread-detail-summary-service.test.js`, `test/thread-detail-performance-service.test.js`, `test/thread-detail-bounded-read-policy-service.test.js`, `test/thread-detail-active-read-policy-service.test.js`, `test/thread-detail-active-window-overlay-policy-service.test.js`, `test/thread-detail-active-window-prewarm-service.test.js`, `test/thread-detail-active-overlay-provider-service.test.js`, `test/thread-detail-active-overlay-integration.test.js`, `test/thread-detail-read-orchestration-service.test.js`, `test/thread-detail-response-budget-service.test.js`, `test/thread-detail-route-service.test.js`, `test/thread-detail-projection-input-service.test.js`, `test/thread-detail-projection-result-service.test.js`, `test/thread-detail-projection-service.test.js`, `test/thread-visible-item-normalizer.test.js`, `test/thread-detail-projection-v4-service.test.js`, `test/message-timestamp.test.js`; include turn operation-detail retention, thread-detail item merge/state, thread/turn merge orchestration, v4 projection merge-state, visible-item patch planning, visible-item/live-text/turn-level DOM patch execution, visible item insert anchoring, conversation HTML update planning, local DOM patch completion planning, keyed child DOM reconciliation, turn article insertion anchoring, lookup, creation, hydration orchestration, single-thread full-render shell planning, thread-detail click-action recognition, thread-tile interaction recognition, pane-state/operation-bubble normalization, operation card content/template planning, operation mode toggle planning, operation dock render-branch planning, pane refresh planning, pane render frame scheduling, pane patch preflight planning, pane patch completion planning, pane slot mutation planning, pane slot mutation side-effect planning including pane count/close effects, pane count/close planning, active pane sync planning, candidate pane id planning, switch-menu option/control planning, explicit selected-pane action/effect planning, detail-load queue/abort/defer planning, detail-load lifecycle side-effect planning, visible-text render identity, local-only retention policy, compact bubble dwell/pinned/recall state policy, explicit empty-completion diagnostics, receipt-only item selection, detail summary lookup/refresh ordering, thread-detail timing diagnostics, bounded-read, active-read, active-window overlay provider/prewarm and integration decisions, detail response operation/reasoning/assistant budget and visible-key rebuilds, read-orchestration and route branch ordering, projection cache-signature input/result fields, v4 pending-overlay, stable visible-key, context-compaction, uploaded-image, and completion-patch coverage when changing projection or UI merge behavior |
| continuation | `test/continuation-lineage.test.js`, `test/continuation-handoff-compaction-service.test.js` |
| context and bootstrap size policy | `test/message-input-service.test.js`, `test/continuation-lineage.test.js`, `test/turn-usage-summary-service.test.js` |
| Workspace token usage ledger | `test/token-usage-stats-service.test.js`, `test/turn-usage-summary-service.test.js`, `test/mobile-viewport.test.js`; include historical Windows mojibake cwd rows when changing Workspace grouping/normalization |
| thread goals | `test/thread-goal-service.test.js`; include sqlite row normalization, list/detail server decoration, `POST /api/threads/:id/goal` app-server `thread/goal/set`, `POST /api/threads/:id/goal/actions` continue/pause/cancel behavior, continuation goal migration planning/wiring, frontend `thread/goal/*` notification handling, `/g` goal dialog submission/action handling through the goal routes, submitted-goal card fallback display when the set response lacks a public goal object, and shell-cache version assertions when changing goal display or entry behavior |
| PWA/update/mobile viewport | `test/mobile-viewport.test.js`, `test/app-update.test.js`, `test/build-refresh-policy.test.js`, `test/thread-performance-metrics.test.js`, `test/thread-tile-layout.test.js`, `test/thread-tile-layout-ui.test.js`, `test/thread-tile-state.test.js`, `test/tablet-layout.test.js`, `test/manual-restart-ui.test.js`, `test/public-pull-request-service.test.js`; include the in-app Restart confirmation panel, running-session risk check, newer-client/older-server build-loop guard, asset-only refresh-prompt suppression, performance-event field extraction, wide-screen thread tile layout/state policy, read-only tile UI wiring, thread-tile visual fixture model/privacy coverage, empty cached-current detail visual-harness smoke wiring, and fixed Public PR review thread reuse when changing restart, refresh, tile layout, or public-PR UI |
| thread visibility/worktree filtering | `test/thread-visibility.test.js`, `test/thread-list-fallback-baseline-service.test.js`, `test/thread-list-fallback-cache-service.test.js`, `test/thread-list-fallback-prewarm-service.test.js`, `test/thread-list-app-server-fetch-policy-service.test.js`, `test/thread-list-response-coalescer-service.test.js`, `test/thread-list-cold-path-diagnosis-service.test.js`, `test/phase-b-readback-smoke.test.js`, `test/phase-b-readback-decision-service.test.js`, `test/thread-status-hints.test.js`, `test/thread-archive.test.js`, `test/mobile-archive-index-service.test.js`, `test/mobile-viewport.test.js`; include archived-session filtering for state DB/session-index fallback paths, Mobile local archived-thread id filtering, rollout-session recovery when `state_5.sqlite` is unavailable or malformed, rollout fallback `updatedAt` / active/completed status inference, newest-session-first rollout discovery under the candidate cap, fallback baseline source-order/timing/count coverage, baseline final-filter/merge work counters, same-read source-context reuse, memory-only source snapshot reuse across final-list filter cache misses, prewarm source-snapshot coverage for wider same-scope first-paint requests, process-lifetime fallback cache hit/miss/expired decision diagnostics, startup prewarm lifecycle, app-server request-window policy, default full-list response coalescing, app-server latency attribution fields, bounded thread-list cold/warm owner attribution, Phase B readback smoke privacy bounds and root-cause decision classification, TTL/incremental updates, replay-aware running/unread status freshness, rollout `session_meta` agent metadata suppression, and UUID-only non-live Mobile fallback residue filtering |
| uploads/files/images | `test/image-compressor.test.js`, `test/message-input-service.test.js`, `test/generated-image-cache-service.test.js`, `test/file-preview.test.js`, `test/file-preview-ui.test.js`, `test/composer-draft.test.js` |
| Hermes Mobile plugin mode | `test/hermes-plugin-service.test.js`, `test/hermes-plugin-route.test.js`, `test/hermes-notification-delegate-service.test.js`, `test/plugin-embed.test.js`, `test/plugin-voice-input.test.js`, `test/home-ai-diagnostic-reporting.test.js` |
| Home AI autonomous delivery return events | `test/home-ai-autonomous-delivery-return-service.test.js`, `test/thread-task-card-service.test.js`; include terminal return-card event payload bounds, trusted web-key callback path, unknown task-card 404 recording, manual return statuses, autonomous auto-return, duplicate/idempotent return processing, and acknowledgement-loop prevention |
| Mac production deployment harness | `test/macos-plugin-deploy-harness.test.js`; include public archive source, private/runtime excludes, staging-before-sync ordering, target-tests-before-restart ordering, and no key-material output when changing deploy scripts |
| cross-thread task cards | `test/thread-task-card-harness.test.js`, `test/thread-task-card-service.test.js`, `test/thread-task-card-routing-service.test.js`, `test/thread-task-card-route.test.js`, `test/codex-mobile-mcp-server.test.js`, `test/conversation-render.test.js`, `test/workspace-source-write-guard-service.test.js`; include readable-text/encoding-damage, approve-in-flight, source-thread direct approval bypass coverage, manual return for approved implementation cards, terminal return-card metadata/no-reply/no-ack-loop behavior, Home AI delivery-loop return-card event observer behavior, MCP `return_to_source` coverage, exact id/title/workspace target resolution, same-workspace distinct-thread delivery, archived/hidden/subagent/sidecar rejection, autonomous workflow same-pair coverage, automatic completion return idempotency, stable source-draft settlement, queued source draft lookup across earlier non-draft messages, silent source auto-send, disabled local workspace-delegation compatibility route coverage, server-side draft materialization on `turn/completed` and thread-detail reads, 8k draft-body truncation before create, thread-detail task-card summary cards with full body available through card GET, target-side-only pending render coverage, and dynamic source-write guard coverage for current `.git`, foreign source file changes, write-like commands, and read-like commands when changing sender or store paths |
| current-thread side chat | `test/thread-side-chat-service.test.js`, `test/thread-side-chat-route.test.js`, `test/collab-agent-render.test.js`, `test/mobile-viewport.test.js`; include server-side-only persistence, per-profile/thread isolation, draft restore, transcript bounds, candidate queue/cancel/apply idempotency, queued auto-send, corrupt store recovery, route wiring, combined left-swipe panel rendering, mobile panel layout, and no browser-local storage fallback when changing side-chat storage, routes, or UI |
| ChatGPT Pro planner / MCP connector | `test/chatgpt-pro-bridge-service.test.js`, `test/chatgpt-pro-planner-service.test.js`, `test/chatgpt-pro-mcp-service.test.js`; include separate MCP bearer auth, JSON-RPC initialize/tools/list/tools/call, artifact runtime persistence, allowlisted documentation reads, pending-default task-card delegation, direct-delegation gate coverage, and regression coverage that no source-write/shell tool is exposed |
| Push | `test/push-notification-service.test.js` |
| runtime settings | `test/runtime-settings.test.js`, `test/composer-quota.test.js`, `test/new-thread-route.test.js`, `test/thread-task-card-route.test.js` |
| Codex profile switching | `test/codex-profile-service.test.js`, `test/codex-profile-ui.test.js`, `test/manual-restart-ui.test.js`, `test/mobile-viewport.test.js`, `test/new-thread-ui.test.js`; include safe account display, active profile persistence, fixed-endpoint switch disablement, browser quota cache clearing, account-scoped quota fallback, explicit `profileId` / `codexHome` restart arguments, windowless auth/config preservation, non-default profile shared-state link list, and `docs/MULTI_ACCOUNT_CODEX_CLI.md` regression coverage |
| scroll and markdown | `test/conversation-scroll.test.js`, `test/turn-scroll-controls.test.js`, `test/markdown-render.test.js`, `test/conversation-render.test.js`, `test/mobile-viewport.test.js`; include thread-detail older-history top pagination, scroll-position preservation after prepending turns, bottom-follow lease evaluation, bottom-follow retry schedule planning, user-reading-current-turn planning, auto-scroll hold update planning, local-patch scroll completion planning, single-thread full-render bottom-follow planning, operational-turn final-receipt deferred rendering, and long final-receipt start positioning |
| GitHub link previews | `test/github-link-preview-service.test.js`; include URL parsing, REST endpoint construction, and bounded preview payload normalization |

Use focused tests first for local iteration, then run `npm.cmd test`, `npm.cmd run check`, `npm.cmd run check:macos`, and `git diff --check` before commit/push or release.

## Ownership Boundaries

- `server.js` owns HTTP protocol shape and app-server request sequencing, but domain-specific state decisions should live in adapters.
- `codex-app-server-mux.js` owns shared-stream mechanics. Do not move browser UI logic into the mux.
- `public/sw.js` owns only cache and Push behavior. Do not put app state or route logic there.
- `.agent-context/` owns durable local context. Do not copy it to public release.
- Runtime files under `%USERPROFILE%\.codex-mobile-web` and `%USERPROFILE%\.codex` are not source files.

## Adding A New Module

1. Prefer `adapters/<domain>-service.js` for server-side policy or state logic.
2. Prefer `public/<domain>.js` for reusable browser helpers.
3. Add focused `node --test` coverage close to the behavior.
4. Add the new JS file to `package.json` `check` if it should be syntax-checked.
5. Document the module here when it becomes part of a stable boundary.
