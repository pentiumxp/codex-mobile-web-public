{
  "name": "codex-mobile-web",
  "version": "0.1.0",
  "description": "A local authenticated mobile web client for Codex app-server.",
  "type": "commonjs",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check codex-app-server-mux.js && node --check public/app.js",
    "check:macos": "bash -n start-codex-mobile-web-macos.sh start-codex-desktop-shared-macos.sh codex-app-server-mux-macos.sh"
  },
  "keywords": [
    "codex",
    "mobile",
    "web",
    "app-server",
    "tailscale"
  ]
}
