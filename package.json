{
  "name": "codex-mobile-web",
  "version": "0.1.4",
  "description": "A local authenticated mobile web client for Codex app-server.",
  "type": "commonjs",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check codex-app-server-mux.js && node --check public/api-client.js && node --check public/runtime-settings.js && node --check public/draft-store.js && node --check public/markdown-renderer.js && node --check public/app.js && node --check public/sw.js",
    "check:macos": "bash -n start-codex-mobile-web-macos.sh start-codex-desktop-shared-macos.sh codex-app-server-mux-macos.sh start-codex-shared-mobile-macos.sh"
  },
  "keywords": [
    "codex",
    "mobile",
    "web",
    "app-server",
    "tailscale"
  ],
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
